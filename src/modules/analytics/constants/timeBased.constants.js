export const GROUP_BY = {
  MONTH: "month",
  YEAR: "year",
};

export const DEFAULT_TREND_LIMIT = 12; // default to last 12 months

export const DATE_FORMAT = {
  MONTH_YEAR: "%Y-%m", // format for trend labels
};
